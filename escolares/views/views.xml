<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="escolares.list">
      <field name="name">Escolares list</field>
      <field name="model">escolares.escolares</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="escolares.action_window">
      <field name="name">Escolares window</field>
      <field name="res_model">escolares.escolares</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="escolares.action_server">
      <field name="name">Escolares server</field>
      <field name="model_id" ref="model_escolares_escolares"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Escolares" id="escolares.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="escolares.menu_1" parent="escolares.menu_root"/>
    <menuitem name="Menu 2" id="escolares.menu_2" parent="escolares.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="escolares.menu_1_list" parent="escolares.menu_1"
              action="escolares.action_window"/>
    <menuitem name="Server to list" id="escolares" parent="escolares.menu_2"
              action="escolares.action_server"/>
    -->
  </data>
</openerp>